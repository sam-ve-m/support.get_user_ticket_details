apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: get-ticket-details-fn-vs
  namespace: fission
spec:
  hosts:
    - hom.api.siga.me
  gateways:
    - homolog/hom-istio-gateway
  http:
  - match:
    - uri:
        prefix: /router/support/get-ticket-details
    rewrite:
      uri: /support/get-ticket-details
    route:
    - destination:
        host: router.fission.svc.cluster.local
        port:
          number: 80